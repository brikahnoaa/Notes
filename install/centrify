2022-09-29
.0 remove
 :dargle: scp ~/Linux/opt/centrify/* station1:
 vi /etc/ssh/sshd_config
PermitRootLogin yes
 systemctl restart sshd
 yum remove -y samba samba-client samba-winbind samba-winbind-clients
.. uninstall
 cd /opt/centrify/
 ./install.sh
  E erase  N no-reboot
.. test root login
.. delete machine from AD
.1 centrify (CAC, ssh)
..1 server-suite
 cd /opt/centrify
 rm *; rm -rf ac
 cp /etc/samba/smb.conf .
 mv /home/kahn/de* .
 tar xzf *-server-suite*z
 tar xzf *adbind*z
 ./install.sh
>> Defaults, adbind=Y, pmel.noaa.gov, Newport Linux, workst=Y, hatfield2
>> reboot=N
..2 AD move on hatfield2
pmel/Computers -> pmel/Pmel Systems/Linux
pmel/Computers -> pmel/Pmel Systems/Servers - Non Windows
properties:comment
..3 Centrify Access Manager on hatfield2
zones/global/child/newport/computers/SYS/role
add:login..newport kahn,others
..4 verify AD, ssh login
 adflush /force
 adquery user kahn
  :dargle: ssh $SYS
 vi /etc/ssh/sshd_config
 systemctl restart sshd
..5 cac test at console

.2 set up samba
 yum install --disableexcludes=all -y samba samba-client samba-winbind samba-winbind-clients
 cp /opt/centrify/smb.conf /etc/samba/smb.conf
  -or-
 :dargle: scp ~/Linux/etc/samba/smb.conf.tmpl $SYS:
 >> smb.conf
    netbios name = station1
.. adbind
/usr/share/centrifydc/bin/adbindproxy.pl
>> /usr, defaults
.. enable, start
  systemctl enable smb winbind
  systemctl restart smb winbind
.3 details -- ./install.sh


*****                                                                *****
*****         WELCOME to the Delinea Server Suite installer!         *****
*****                                                                *****

Detecting local platform ...

Running ./adcheck-rhel6-x86_64 ...
OSCHK    : Verify that this is a supported OS                          : Pass
PATCH    : Linux patch check                                           : Pass
DEPS     : Dependency check                                            : Pass
PERL     : Verify perl is present and is a good version                : Pass
SAMBA    : Inspecting Samba installation                               : Pass
NSCD     : Check if Name Service Caching Daemon is running             : Warning
         : Name Service Caching Daemon is not running

SPACECHK : Check if there is enough disk space in /var /usr /tmp       : Pass
HOSTNAME : Verify hostname setting                                     : Pass
NSHOSTS  : Check hosts line in /etc/nsswitch.conf                      : Pass
DNSPROBE : Probe DNS server 161.55.192.11                              : Pass
DNSPROBE : Probe DNS server 161.55.124.21                              : Pass
DNSCHECK : Analyze basic health of DNS servers                         : Warning
         : One or more DNS servers are dead or marginal.
         : Check the following IP addresses in /etc/resolv.conf.
         :
         : The following table lists the state of all configured
         : DNS servers.
         :  161.55.124.21 (netservices.pmel.noaa.gov): marginal
         :  161.55.192.11 (netservicesnewp.pmel.noaa.gov): OK
         : Only one good DNS server was found
         : You might be able to continue but it is likely that you
         : will have problems.
         : Add more good DNS servers into /etc/resolv.conf.

WHATSSH  : Is this an SSH that Delinea's DirectControl Agent works well with: Pass
SSH      : SSHD version and configuration                              : Warning
         : You are running OpenSSH_7.4p1, OpenSSL 1.0.2k-fips  26 Jan 2017.
         :
         : This version of OpenSSH does not seem to be configured for PAM,
         : ChallengeResponse and Kerberos/GSSAPI support.
         : To get Active Directory users to successfully login,
         : you need to configure your OpenSSH with the following options:
         : (display the ones we identified were not set)
         : ChallengeResponseAuthentication yes
         : UsePAM Yes
         :
         : Delinea provides a version of OpenSSH that's configured properly
         : to allow AD users to login and provides Kerberos GSSAPI support.

3 warnings were encountered during check. We recommend checking these before proceeding

WARNING: adcheck exited with warning(s).

This installation script provides installation of the following services
in Delinea Server Suite on UNIX and Linux:
 - Authentication Service
 - Privilege Elevation Service
 - Auditing & Monitoring Service

The Authentication Service and Privilege Elevation Service
are contained in the CentrifyDC (DirectControl) packages,
and the Auditing & Monitoring Service is in the CentrifyDA
(DirectAudit) packages.

With this script, you can perform the following tasks:
    - Install (update) CentrifyDC & CentrifyDA packages (License required) [E]
    - Install (update) CentrifyDC only packages (License required) [S]
    - Install (update) CentrifyDC Express packages [X]
    - Custom install (update) of individual packages [C]

You can type Q at any prompt to quit the installation and exit
the script without making any changes to your environment.

How do you want to proceed? (E|S|X|C|Q) [E]: c

Install the CentrifyDC 5.9.1 package? (Q|Y|N) [Y]:

INFO: Pre-selecting CentrifyDC-openssl (required by CentrifyDC-5.9.1) ...

INFO: Pre-selecting CentrifyDC-openldap (required by CentrifyDC-5.9.1) ...

INFO: Pre-selecting CentrifyDC-curl (required by CentrifyDC-5.9.1) ...

Install the CentrifyDC-ldapproxy 5.9.1 package? (Q|Y|N) [N]:

Install the CentrifyDC-nis 5.9.1 package? (Q|Y|N) [N]:

Install the CentrifyDC-cifsidmap 5.9.1 package? (Q|Y|N) [N]:

Install the CentrifyDC-openssh 5.9.1 package? (Q|Y|N) [N]:

Install the CentrifyDA 5.9.1 package? (Q|Y|N) [N]:

Install the CentrifyDC-adbindproxy 5.9.0 package? (Q|Y|N) [N]:y

The Express mode license allows you to install a total of 200 agents.
The Express mode license does not allow the use of licensed features for
advanced authentication, access control, auditing, and centralized
management.  This includes, but is not limited to, features such as
SmartCard authentication, Privilege Elevation, Auditing, Group Policy,
Login User Filtering, and NSS overrides.

Do you want to install in Express mode? (Q|Y|N) [N]:

Do you want to run adcheck to verify your AD environment? (Q|Y|N) [Y]:

Please enter the Active Directory domain to check []: pmel.noaa.gov
Join an Active Directory domain? (Q|Y|N) [Y]:
    Enter the Active Directory domain to join [pmel.noaa.gov]:
    Enter the Active Directory authorized user [administrator]:
    Enter the password for the Active Directory user:
    Enter the computer name [caldera.pmel.noaa.gov]:
    Enter the container DN [Computers]:
    Enter the name of the zone: Newport Linux
    Join domain using workstation license type? (N for server license) (Q|Y|N) [N]: y
    Enter the name of the domain controller [auto detect]: hatfield2
Reboot the computer after installation? (Q|Y|N) [Y]:n

You entered the following:
    Install CentrifyDC 5.9.1 package: Y
    Install CentrifyDC-openssl 5.9.1 package: Y
    Install CentrifyDC-openldap 5.9.1 package: Y
    Install CentrifyDC-curl 5.9.1 package: Y
    Install CentrifyDC-ldapproxy 5.9.1 package: N
    Install CentrifyDC-nis 5.9.1 package: N
    Install CentrifyDC-cifsidmap 5.9.1 package: N
    Install CentrifyDC-openssh 5.9.1 package: N
    Install CentrifyDA 5.9.1 package: N
    Install CentrifyDC-adbindproxy 5.9.0 package: Y
    Express mode                     : N
    Run adcheck                      : Y
    Join an Active Directory domain  : Y
    Active Directory domain to join  : pmel.noaa.gov
    Active Directory authorized user : administrator
    computer name                    : caldera.pmel.noaa.gov
    container DN                     : Computers
    zone name                        : Newport Linux
    license type                     : workstation
    domain controller name           : hatfield2
    Reboot computer                  : N


.4 existing users
 cd /home; ls -ln 
 adquery user *
   grep cnsd /etc/passwd /etc/group
   chown -R 469.110 /home/cnsd /var/spool/mail/cnsd
 cd /var/spool/mail; ls -ln
    a="adquery user"; i=kahn
    echo chown -R $($a -g $i).$($a -u $i) /home/$i /var/spool/mail/$i
    # chown -R 1256.1100 /home/kahn /var/spool/mail/kahn
# ditto

---
### integrate existing to AD
---
.x chown
note: wilkinso baumberg
.. /home, /users:
 chown -R root.root *
 us=$(adquery group --members newport)
 for i in $us; do if [ -d $i ]; then chown -R $i.newport $i; fi; done
 # chown -R cnsd.users cnsd
.. /var/mail
 chown root *
 for i in $us; do if [ -f $i ]; then chown $i $i; fi; done
.. /data*
 chgrp -R newport *
 chmod -R o-rwx *
 chmod g-w *
## make these changes on challenger:/backup too
syspc:
 find /home /data0 /data1 -uid 203 -print0 | xargs -0 chown lau.newport
---

.9 debug
systemctl --all list-units
