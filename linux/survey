Summary: 
3 Racks = 2 full height, 1 half height, all with APC UPS
11 Servers = 3 CNSD Windows, 3 CNSD Redhat, 5 Project Redhat 
Network = 2 Cisco LAN, 1 Cisco Metro WAN, 2 Fiber patch panels
Monitor = 1 screen / keyboard / trackball, 2 KVM switches

CNSD owned servers:
nserver - previous domain server, plan to excess 2015-Q2
nserver1 - backup domain controller, print server, file server
newpserv - primary domain controller, print server
oerdftp - ftp staging, web staging
syspc - general purpose; minor development, data feeds
ventstore - server and PC restore

Project owned servers:
acoustic (Acoustic) - 12TB live data, 24TB disk mounted, 40TB max
bioac2 (shared) - daily backup, archival mount point
bioac3 (Acoustic) - 36TB live data, 64TB disk mounted, 80TB max
caldera (shared) - license server, matlab, IDL
embfile2 (EOI) - 12TB live data, 24TB disk mounted, 40TB max
