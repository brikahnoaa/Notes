reg add HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System /v scforceoption /d 0 /f
query user

\\pesto\cnsd\ops\Tools\McAfee tool\EEAdminTools
C:\Users\cnsd_local\Downloads\EpeTemporaryAutoboot.exe --number-of-reboots 5 --timeout-in-minutes 300

trout_sb pme!@2739 (end of prop label)
lusrmgr.msc  user manager

sherlock nessus ness_report:n3ss@cnsd

At this time it is understood that not all external drives are marked with 
a property tag. Do not register your personal drives, you can register 
drives purchased for NOAA work.
- right-click Start (bottom left corner Windows icon)
- select Device Manager
- expand "Disk drives"
- right-click the USB drive and choose Properties
- select Details tab in Properties windows
- drop-down Property and select "Device instance path"
- copy the whole string in Value.
Open a CNSD ticket with this info and brief description 
 (brand, model, thumb or external disk)

UPS bkahn@noaa:MM!!/12 
.. w8R3UAP-PAB ac, w8R3UER-PGC gas, w8R3UER-PBS eoi

CAC id <- activcard[O]:open:certs:authen:subjAlt

# smb on ac b3 e2 sc cr ve
 smbstatus
yum -y --disableexcludes=all update
 systemctl restart centrifydc-samba.service;  \
 systemctl status centrifydc-samba.service

purch: sharon walker, josh, ben, rusty

join domain - system::advanced::domain
  sysdm.cpl
powershell:
  add-computer –domainname pmel -Credential pmel\administrator -restart –force

query user   = cli to see who is logged on
eusrmgr.msc  = user mgr

takeown /R /F *
icacls * /t /q /c /reset
icacls * /t /c /grant:F
icacls * /t /c /grant kahn:F

hire date 8/18/2014

register LAPS:
msiexec /i "\\pesto\gpo$\LAPS\LAPS.x64.msi" /q

office:
setup.exe /configure configuration.xml

vpn:
msiexec -i PulseSecure.x64-9_1_9.msi CONFIGFILE="Win-AD-CP.pulsepreconfig" /qb
cd "C:\Program Files (x86)\Common Files\Pulse Secure\JamUI"
.\jamCommand -importFile C:\Users\cnsd_local\Desktop\Win-AD-CP.pulsepreconfig
https://remote.pmel.noaa.gov/pmel-vpn-wincp

control panel = appwiz.cpl

cac waive group=CAC Enforcement Exception (use password)

requested: actual: people:

The NRS data that are required for screening are 02, 03, 04, 05, 07 & 08
email data info such as the hydrophones' locations and depth
new NRS02 and BRS03 data are required to be screened by the Navy

 :rsync:
install cygwin with rsync 
  ldd /usr/bin/rsync | grep -v WIND | cut -f3 -d' '
---
cygwin-lite http://cygwin-lite.sourceforge.net/faq.html
---
osu wifi login
Wi-Fi Security: "WPA & WPA2 Enterprise"
Authentication: "Protected EAP (PEAP)"
Anonymous identity: blank
Domain: Enter "oregonstate.edu"
CA certificate: "(None)"
CA certificate password: blank
 No CA certificate is required: Check 
PEAP version: "Automatic"
Inner authentication: "MSCHAPv2"
Username: ONID username 
  eduroam: followed by "@oregonstate.edu" 
Password: ONID password here

robocopy backup
robocopy C:\src M:\dst /MIR /S /FFT /R:3 /W:10 /Z /NP /NDL /XJD /MT:8
Parameters explained
The /MIR option (equivalent to /PURGE /E) stands for "mirror" and is the most important option. It regards your source folder as the "master", causing robocopy to copy/mirror any changes in the source (new files, deletions etc.) to the target, which is a useful setting for a backup.
/FFT is a very important option, as it allows a 2-second difference when comparing timestamps of files, such that minor clock differences between your computer and your backup device don't matter. This will ensure that only modified files are copied over, even if file modification times are not exactly synchronized.
/R:3 specifies the number of retries, if the connection should fail, and /W:10 specifies a wait time of 10 seconds between retries. These are useful options when doing the backup over a network.
/Z copies files in "restart mode", so partially copied files can be continued after an interruption.
/NP and /NDL suppress some debug output, you can additionally add /NS, /NC, /NFL to further reduce the amount of output (see the documentation for details). However, I would suggest to print some debug output during the first runs, to make sure everything is working as expected.
Additional useful parameters mentioned by other users
/XJD excludes "junction points" for directories, symbolic links that might cause problems like infinite loops during backup. See Brian's comments for details.
/MT[:N] uses multithreading and can speed up transfers of many small files. For N, a value of 2-4 times the number of cores should do on a normal machine. Commented by Zoredache on the original question.

reset updates
https://pureinfotech.com/reset-windows-update-windows-10-fix-downloads-installs/
dism.exe /Online /Cleanup-image /Restorehealth
sfc /scannow
net stop wuauserv
net stop cryptSvc
net stop bits
net stop msiserver
rmdir /q /s C:\Windows\SoftwareDistribution.old
rmdir /q /s C:\Windows\System32\catroot2.old
ren C:\Windows\SoftwareDistribution SoftwareDistribution.old
ren C:\Windows\System32\catroot2 catroot2.old
Del "%ALLUSERSPROFILE%\Application Data\Microsoft\Network\Downloader\*.*"
net start wuauserv
net start cryptSvc
net start bits
net start msiserver

beyond trust challenge code shared secret : noaapmel//^0@^1

.2 win10 win11
 wmic product get name
 wmic /node: product get name
Once you know the name of the software, you can remove it with wmic as well:
 wmic product where name="application name" call uninstall
.. 
C:\> wmic /node:brutal product get name,version /format:csv > sw.csv
.. prod key (dargle)
... wmic path softwareLicensingService get OA3xOriginalProductKey
OA3xOriginalProductKey
B3NH4-4T2RY-XK3VD-GM8D4-F6YP2
... powershell "(Get-WmiObject -query 'select * from SoftwareLicensingService').OA3xOriginalProductKey"
... \HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\SoftwareProtectionPlatform\BackupProductKeyDefault
.. prod key (lab rat OSU)

. why PC has reboot sched
   c:\windows\ccm\logs\RebootCoordinator.log
(you may want to use c:\windows\ccm\CMTrace.exe to view files)
Search for the following string: requested from

Most of the time a new CAC works the same as before. I see that your account needs to be updated with the 16 dig ID, it would be better to do that before you get the new one. Your ID is found one of two ways:
.1 log out, and before logging in look at the display of name/ID for each of the icons under SignInOptions. Find the one with 16 digs probably ending in ...113305@mil.gov
.2 open Menu::ActivID::UserConsole and select MyCertificates::Authentication::Details::SubjectAltName, look for that same data.
  Let me know for sure your ID ends in those 6 digs. Yours starts with 1388396357 in the first 10 digs.
The 10 dig ID may not work with the new CAC, so let's fix this first.
  Also, note that you can get to google meet and all without your CAC using any computer, but if you don't have a personal computer using your phone browser can be awkward.

windows store
1.  Run WSReset.exe.
2.  Re-register Windows Store by running the following commands in a CMD window started as Administrator:
  PowerShell -ExecutionPolicy Unrestricted
  $manifest = (Get-AppxPackage Microsoft.WindowsStore).InstallLocation + '\AppxManifest.xml' ; Add-AppxPackage -DisableDevelopmentMode -Register $manifest
--
  Get-AppXPackage WindowsStore -AllUsers | Foreach {Add-AppxPackage -DisableDevelopmentMode -Register "$($_.InstallLocation)\AppXManifest.xml"}
--
https://www.maketecheasier.com/windows-store-not-working-fix/

Ron: activclient 7.2 - 7.4 broke (should uninstall first)
put system into pmel.noaa.gov:isolate OU, then gpupdate /force
examine group policy with rsop.msc from cmd admin
net user to reset cnsd_local pwd if LAPS is broke: net user cnsd_local NEWPASS

git update-index --skip-worktree [path] to stop git from tracking changes
git update-index --no-skip-worktree [path] to start tracking that file again

2115 SE OSU Drive
Newport, Oregon 97365

purchase: rusty robertSlaney
